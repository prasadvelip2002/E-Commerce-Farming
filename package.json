{
  "name": "agri-ecommerce-platform",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "napi-postinstall": "^0.3.4",
    "turbo": "^2.0.0"
  },
  "overrides": {
    "react-native": "0.81.5"
  }
}
